# Janitor Log

## 2026-01-27
- Configured linters (golangci-lint) and fixed linting errors in `block.go` and `main.go` (checked return values of `fmt` functions).
- Set up `mise` for task management.
- Standardized GitHub Actions workflow.
